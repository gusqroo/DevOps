name: build-docker-image

on: [push, pull_request]

jobs:
    job1:
        name: First job
        runs-on: ubuntu-latest
        steps:
        - name: Step One
          uses: actions/checkout@v3
        - name: Step two
          run: |
            echo "Testing"
            pwd